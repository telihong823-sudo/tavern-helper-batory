# 设计构思：巴托里城堡 · 状态栏

## 输入

- **schema.ts**：`D:\卡\cards\巴托里城堡\schema.ts`
  - `莉奥娜`: `情欲值`(0~100), `心声`(string ≤240)
  - `薇拉`: `依赖值`(0~100), `心声`(string ≤240)
- **项目上下文**：哥特西幻角色卡《巴托里城堡》。莉奥娜：吸血鬼贵族家主，银白长发、红瞳、傲慢嘴硬，情欲值四阶段；薇拉：保留意识的尸仆，白丝女仆装、迟钝呆滞，依赖值三阶段。开场两人都在场。
- **用户需求（原话归纳）**：前端版状态栏，不复杂。一个人在场就写那一个人的姓名、阶段、当时数值（不需要很显眼）、心声；两个人在场就在下面加第二个人的。

## 视觉基调

### 感官词

烛火、月光、银器、暗红（莉奥娜）、白丝（薇拉）、低语。

氛围像城堡墙角的烛台：存在，但安静，不抢戏。不做头像、不做进度条动画、不做图标装饰。

### 交互人格

**低语式**：语速慢、音量小、动作轻。状态栏默认就是展开的完整信息，但整体视觉压低对比度——小字号、低饱和、muted 心声。它不是面板，是贴在楼层里的一行小字。

### 语义配色

| 变量 | 色值 | 语义来源 | 用途 |
|------|------|---------|------|
| --c-text | 银灰白（如 #d8d3c8） | 月光照在银器上 | 姓名 |
| --c-text-muted | 暗灰（如 #8a8578） | 烛火照不到的角落 | 心声、分隔 |
| --c-border | 极淡暖灰（如 rgba(200,190,170,0.18)） | 烛火边缘的微光 | 区块描边 |
| --c-accent-leona | 暗红（如 #a03c3c） | 莉奥娜的红瞳 | 莉奥娜阶段/数值 |
| --c-accent-vira | 暖白（如 #c8b8a0） | 薇拉的白丝/银链 | 薇拉阶段/数值 |

背景：无（卡片形状、消息楼层内嵌，不强行加底色）。色值可在编码阶段微调，语义理由不变。

## 组件构思

### 信息优先级

- **始终可见（每在场角色）**：姓名 + 阶段标签 + 数值小字
- **次级（同一区块第二行）**：心声（muted、斜体、小字）
- **情境**：无（不做条件显隐，保持简单）

### 布局模式

叙事型（单列纵向）。每个在场角色一个窄区块，从上到下排列：

```
莉奥娜 · 拘谨期 · 34        ← 姓名(正常) 阶段标签(accent) 数值(小字 muted)
  那家伙的手指……太近了。       ← 心声(斜体 muted 小字)
───────────────
薇拉 · 淡漠期 · 12
  来应聘的新人。脚步放得很轻。
```

双人在场时第二个区块自然追加在下方，中间用极淡分隔。

### 在场判定

**心声非空 = 在场**（依赖 schema 约定：变量更新规则中「不在场 → 置为 ""」；initvar 初始两人心声非空，对应开场两人在场）。不新增 schema 字段，不做在场布尔。

### 阶段映射（编码时写死映射函数）

- 莉奥娜·情欲值：0~25 拘谨期 / 26~50 松动期 / 51~75 主动期 / 76~100 放浪期
- 薇拉·依赖值：0~30 淡漠期 / 31~60 依赖期 / 61~100 粘人期

### 数值展示

- 莉奥娜：`情欲值` 数字（0~100），标签「情欲」或省略标签只留数字+阶段。**用户要求不显眼** → 数值用小字 muted，阶段标签用 accent 色小字。
- 薇拉：`依赖值` 数字（0~100），同上。
- 不显示「/100」进度，不做条。

## 组件树

```
src/巴托里城堡/界面/状态栏/
├── index.ts          # 入口，等 MVU 初始化后挂载
├── index.html        # 仅 <div id="app"></div>
├── App.vue           # 读 store，判定在场，渲染 CharacterBlock 列表
├── store.ts          # defineMvuDataStore(Schema, { type:'message', message_id: getCurrentMessageId() })
├── global.css        # 色彩变量 + 基础排版
└── components/
    └── CharacterBlock.vue  # props: name, stage, value, voice, accent
```

## 编码约束（模板规则，必须遵守）

- iframe 内嵌：禁用 `vh`、`min-height`、`overflow:auto` 撑高；用内容自然高度或 `width + aspect-ratio`。
- 不产生横向滚动条，宽度适配容器。
- 卡片形状界面不加背景色（除非用户明确要求——本设计即无背景）。
- 样式写在 vue 组件 `<style lang="scss">` 或 ts 导入，`index.html` 只留静态 body。
- 字体不额外引入（默认足够）；图标不用。
- `store.data` 即 schema 结构：`store.data.莉奥娜.情欲值`、`store.data.莉奥娜.心声`、`store.data.薇拉.依赖值`、`store.data.薇拉.心声`。

## 验收标准

1. 单人/双人区块随心声非空动态出现，空则不渲染
2. 阶段映射正确（四段/三段）
3. vue-tsc 类型检查通过
4. 视觉：低调、小字、无背景、无横向滚动
